import "./styles.css";

const chartsRootDiv = document.getElementById("app-content");

// Add contents
const container: HTMLElement = document.createElement("div")!;
chartsRootDiv!.appendChild(container);
